{
  "name": "ftpup",
  "version": "0.7.1",
  "description": "Simple FTP uploader for node.js.",
  "author": "lemmon",
  "license": "MIT",
  "bin": {
    "ftpup": "cli.js"
  },
  "dependencies": {
    "basic-ftp": "~5.0.0",
    "glob": "~8.0.0",
    "meow": "~9.0.0"
  },
  "scripts": {
    "version": "git add -A",
    "postversion": "git push && git push --tags && npm publish"
  }
}
